var app = require("../app");
var debug = require("debug")("myshop:server");
var http = require("http");

app.listen(process.env.PORT || 3000, (req, res) => {
  console.log("App is listenig to port 3000");
});
